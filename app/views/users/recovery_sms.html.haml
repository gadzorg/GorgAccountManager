%section.pam
  .w500p.center
    .box.shadow-1.mtm
      .box-top.pam
        On t'a envoyé un sms!
      .box-bottom.pam
        %p.mtm
          = form_tag(validate_sms_path(), :method => 'post') do
            = hidden_field_tag :token_session, @session_token
            = label_tag :token, 'Code SMS:'
            = text_field_tag :token
            = submit_tag 'Vérifier',:class => "bouton bouton_bleu"

        %p.small.mtl 
          Rien reçu sur ton numéro #{@phone_hidden} après quelques minutes? Tu peux rééssayer à nouveau
          = link_to create_sms_path(:token_session => @session_token) , :method=> :post, title: "Envoie-moi un sms" do
            Envoie-moi un sms
        